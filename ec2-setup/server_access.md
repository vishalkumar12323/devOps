# Server Access and firewall strategy

This guide explains how to protect A server, prevent Access Management using Firewalls

## 1) Checking the logs and ufw status
```bash
sudo cat /var/log/auth.log
```